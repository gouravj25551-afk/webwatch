// Project gaps found while auditing the repository at commit 562818e.
// `evidence` is a verified fact; `recommendation` is advice, not existing behaviour.
export default [
  {
    id: 'pricing-mismatch',
    title: 'Code sells a $1 permanent slot, not $2 for two months',
    severity: 'High',
    area: 'Payments',
    files: ['Backend/src/services/dodoPayments.js:44', 'Backend/src/routes/billing.js:40', 'Frontend/src/App.jsx'],
    evidence: 'createCheckoutSession charges 100 cents per slot and fulfillPayment increments User.paidMonitorsCount. The checkout modal says "One-time payment per site monitored".',
    recommendation: 'Model the intended offer explicitly: price 200 cents, and grant an entitlement with an expiry date instead of a permanent counter.',
  },
  {
    id: 'no-subscription-expiry',
    title: 'Paid access never expires',
    severity: 'High',
    area: 'Payments',
    files: ['Backend/prisma/schema.prisma'],
    evidence: 'There is no expiry or period-end column on User, Payment or Monitor. paidMonitorsCount only ever increases.',
    recommendation: 'Add an entitlement record (monitorId or slot, paidUntil) written only by the verified webhook, and have the scheduler skip monitors whose paidUntil has passed.',
  },
  {
    id: 'alert-email-unverified',
    title: 'Alerts can be sent to any email address',
    severity: 'High',
    area: 'Notifications',
    files: ['Backend/src/routes/monitors.js:76', 'Backend/src/services/emailService.js:37'],
    evidence: 'POST /api/monitors accepts any alertEmail that matches a simple regex, and sendAlert emails it directly. The recipient never confirms they want these emails.',
    recommendation: 'Only allow the account email until a recipient clicks a confirmation link. This protects strangers from spam and protects the sending domain reputation.',
  },
  {
    id: 'email-html-unescaped',
    title: 'Monitor name is inserted into email HTML without escaping',
    severity: 'Medium',
    area: 'Notifications',
    files: ['Backend/src/services/emailService.js:14'],
    evidence: 'emailTemplate interpolates monitor.name and monitor.url straight into an HTML string. A name like <a href=...> becomes real HTML in the alert email.',
    recommendation: 'Escape &, <, >, " and \' before inserting user text into HTML.',
  },
  {
    id: 'no-email-verification',
    title: 'Account emails are not verified',
    severity: 'Medium',
    area: 'Authentication',
    files: ['Backend/src/routes/auth.js:37', 'Backend/prisma/schema.prisma'],
    evidence: 'Registration creates the user and signs them in immediately. User has no emailVerified field.',
    recommendation: 'Add verification before enabling alerts or payments.',
  },
  {
    id: 'consecutive-failures-unused',
    title: 'consecutiveFailures is stored but never used for decisions',
    severity: 'Low',
    area: 'Monitoring',
    files: ['Backend/src/services/monitorRunner.js:100'],
    evidence: 'The counter is incremented on failure and reset on success, but an incident opens after the first failed run (three attempts inside one run). No code reads the counter.',
    recommendation: 'Either use it (open an incident after N failed runs) or document that the three in-run attempts are the confirmation rule.',
  },
  {
    id: 'no-alert-retry',
    title: 'A failed alert email is logged and dropped',
    severity: 'Medium',
    area: 'Notifications',
    files: ['Backend/src/services/monitorRunner.js:108'],
    evidence: 'If sendAlert throws, runMonitor only logs "Alert delivery failed". There is no delivery log table and no retry.',
    recommendation: 'Store each alert as a row with a status, and retry pending rows from the worker.',
  },
  {
    id: 'free-check-not-in-ui',
    title: 'The one-time free check exists only as an API',
    severity: 'Low',
    area: 'Product',
    files: ['Backend/src/app.js:41', 'Frontend/src/App.jsx'],
    evidence: 'POST /api/check works without login, but the React app never calls it. It is protected only by the global rate limit.',
    recommendation: 'Decide whether the free check needs an account (as in the product idea) and give it its own stricter limiter.',
  },
  {
    id: 'check-now-no-extra-limit',
    title: '"Check now" has no dedicated rate limit',
    severity: 'Low',
    area: 'Security',
    files: ['Backend/src/routes/monitors.js:106', 'Backend/src/app.js:25'],
    evidence: 'Each click can make up to three outbound requests. Only the global limiter (200 requests per 15 minutes) applies.',
    recommendation: 'Add a per-user limiter or a cooldown based on lastCheckedAt.',
  },
  {
    id: 'webhook-failed-events-ignored',
    title: 'Only payment.succeeded webhooks are handled',
    severity: 'Low',
    area: 'Payments',
    files: ['Backend/src/routes/webhooks.js:35'],
    evidence: 'Failed, cancelled or refunded payment events are acknowledged but not recorded, so PENDING payments stay PENDING.',
    recommendation: 'Handle failure and refund events and update Payment.status.',
  },
  {
    id: 'tests-need-jwt-secret',
    title: '`npm test` fails without a JWT_SECRET',
    severity: 'Low',
    area: 'Testing',
    files: ['Backend/src/config.js:27', 'Backend/test/dodoPayments.test.js'],
    evidence: 'config.js throws at require time when JWT_SECRET is missing. The payments test imports config, so a fresh clone fails. Verified: with a 40-character dummy secret all 7 tests pass.',
    recommendation: 'Set a test secret in the test script or in the test file before requiring config.',
  },
  {
    id: 'few-tests',
    title: 'Only two test files; no route, runner or frontend tests',
    severity: 'Medium',
    area: 'Testing',
    files: ['Backend/test/urlSafety.test.js', 'Backend/test/dodoPayments.test.js'],
    evidence: '7 tests cover URL parsing, IP classification and payment matching. Nothing tests auth, ownership, incident transitions or the scheduler.',
    recommendation: 'Start with pure-logic tests for incident transitions, then route tests for ownership (404 for another user\'s monitor).',
  },
  {
    id: 'worker-no-health',
    title: 'The worker has no health signal',
    severity: 'Medium',
    area: 'Operations',
    files: ['Backend/worker.js'],
    evidence: 'worker.js logs to the console and exits on fatal errors, but exposes no heartbeat or health endpoint. If it stops, nothing notices.',
    recommendation: 'Write a heartbeat timestamp each cycle and alert when it is stale.',
  },
  {
    id: 'logging-console-only',
    title: 'Logging is plain console output',
    severity: 'Low',
    area: 'Operations',
    files: ['Backend/src/middleware/errorHandler.js'],
    evidence: 'Errors go to console.error with no request id or structure.',
    recommendation: 'Fine for the beta; add structured logs before debugging real customer issues.',
  },
  {
    id: 'gh-actions-cron-best-effort',
    title: 'GitHub Actions cron is best-effort',
    severity: 'Medium',
    area: 'Operations',
    files: ['.github/workflows/monitor-scheduler.yml', 'Backend/src/app.js:50'],
    evidence: 'Production checks are triggered by a */5 cron that calls /api/cron. Scheduled workflows can be delayed or skipped when GitHub is busy, and the checks run inside a serverless request.',
    recommendation: 'Move to the dedicated worker for reliable timing, as the README already plans.',
  },
  {
    id: 'readme-lease-outdated',
    title: 'README still says a lease must be added',
    severity: 'Low',
    area: 'Docs',
    files: ['README.md:49', 'README.md:180'],
    evidence: 'Line 49 correctly describes the database lease, but the deployment notes on line 180 still say "Before running multiple workers, add a distributed queue or database claim/lease". The README also describes a $1 plan.',
    recommendation: 'Update the paragraph so it matches the code.',
  },
  {
    id: 'history-uptime-capped',
    title: '"7-day" history uptime uses at most the last 500 checks',
    severity: 'Low',
    area: 'Monitoring',
    files: ['Backend/src/routes/monitors.js:169'],
    evidence: 'GET /api/monitors/:id/history loads checks with take: 500 and computes uptime from that list. At 5-minute checks, 7 days is 2016 checks, so the figure covers roughly the last 41 hours. The 30-day number on the dashboard uses count() and is not capped.',
    recommendation: 'Compute uptime with count() queries like monitorWithStats, and keep take: 500 only for the chart rows.',
  },
  {
    id: 'pause-keeps-incident-open',
    title: 'Pausing a monitor leaves its incident open',
    severity: 'Low',
    area: 'Monitoring',
    files: ['Backend/src/routes/monitors.js:126'],
    evidence: 'PATCH with enabled:false only changes enabled, status and lastCheckedAt. An open incident stays open and is closed (with a recovery email) by the first successful check after resume.',
    recommendation: 'Decide the rule explicitly: close or annotate incidents on pause.',
  },
  {
    id: 'shutdown-drops-lease',
    title: 'Worker shutdown does not wait for running checks',
    severity: 'Low',
    area: 'Operations',
    files: ['Backend/worker.js:6'],
    evidence: 'shutdown() stops the timer, disconnects Prisma and exits. A check in progress is cut off, its finally block never releases the lease, and the monitor waits up to 2 minutes for the lease to expire.',
    recommendation: 'Track in-flight runs and await them (with a time limit) before exiting.',
  },
  {
    id: 'no-check-retention',
    title: 'Check history is never deleted',
    severity: 'Low',
    area: 'Database',
    files: ['Backend/src/services/monitorRunner.js:40'],
    evidence: 'Every run inserts a Check row and no code in Backend/src deletes old rows. One monitor at 5 minutes adds about 8,600 rows a month.',
    recommendation: 'Delete or roll up checks older than the longest history the UI shows (30 days).',
  },
  {
    id: 'worker-hosting-undefined',
    title: 'No hosting setup for the dedicated worker',
    severity: 'Medium',
    area: 'Operations',
    files: ['Backend/worker.js', 'vercel.json'],
    evidence: 'vercel.json deploys the frontend and API only. There is no Dockerfile, Procfile or platform config for worker.js, so production still depends on the GitHub Actions cron.',
    recommendation: 'Pick a small always-on host for the worker and document its start command and env vars.',
  },
  {
    id: 'payment-race-unverified',
    title: 'Two identical webhooks at the same instant may both grant a slot',
    severity: 'Needs verification',
    area: 'Payments',
    files: ['Backend/src/services/dodoPayments.js:96'],
    evidence: 'fulfillPayment reads status, then updates it, inside a transaction at the default isolation level. By reading alone this looks racy for truly simultaneous deliveries; it has not been reproduced.',
    recommendation: 'Make the transition conditional (updateMany where status is PENDING, grant only if count is 1) and write a test with two concurrent calls.',
  },
]
